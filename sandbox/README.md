# Pipeworks sandbox

This directory is copied from `fixtures/` when you run `../tools/reset-sandbox.sh`.

Run lesson commands from this directory unless the lesson says otherwise:

```sh
cd sandbox
```

Write exercise outputs under `out/`.
