# Pipeworks training app config
app_name = pipeworks-demo
environment = training
listen_port = 8080

# Duplicate key kept for search exercises.
feature_checkout = true
feature_checkout = false

api_host = api.internal.test
database_host = old-db.internal
log_level = INFO
