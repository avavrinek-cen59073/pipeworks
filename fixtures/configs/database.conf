# Database configuration
host=old-db.internal
port=5432
database=pipeworks_training
user=training_app
sslmode=require

# Legacy read replica kept for migration practice.
replica_host=legacy-db.internal
replica_port=5433
